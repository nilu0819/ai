# Core (Calculator)
# Standard library only - no extra deps

# Vision Assist Mode
opencv-python>=4.8.0
numpy>=1.24.0
pyttsx3>=2.90
pytesseract>=0.3.10  # Optional: for scan mode (also requires Tesseract OCR installed)
